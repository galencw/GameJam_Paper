# 20260522 改动总结

> 日期：2026-05-22  
> 范围：UI 设计文档 + 规则层 + UI 接入

---

## 一、UI 文档重写（v0.1 → v0.2）

依据新设计图 `docs/20260522-204041.png` 重写以下两份文档：

- `docs/新乡2000_UI界面设计方案.md`
- `docs/新乡2000_UI组件拆分建议.md`

**结构性变化**：

| 项目 | v0.1 | v0.2 |
|------|------|------|
| 左侧资金视觉 | 四色金钱柱（蓝/绿/黄/红） | 左右两个阵营资金面板（对手橙 / 玩家青，镜像结构） |
| 市场情绪条 | K 线下方独立横条 | 顶部信息栏内（红→黄→绿渐变 + 百分比 + 情绪表情） |
| 弹幕 | K 线浮层 | 独立全宽弹幕行 |
| K 线 + 盘口 | 两个面板分开 | 合并到同一外框（左：回合K + 分时K；右：股价/现金/持仓/市值/总盈亏/总资产） |
| 行动力 | 顶部 `X/3` | 底部 3 个胶囊图标 |
| 卡牌 | 简单卡 | 顶部色带 + 名称 + 耗值 + 等级 + 图标 + 描述 |
| 回合控制 | 无独立区 | 底部独立"交易回合 / 结束回合"常驻按钮区 |
| 阵营区 | 简化 | 左下"商战对手"、右下"玩家"完整面板 |

组件拆分文档同步重排为 7 大类切图清单，重排 P0/P1/P2 优先级。商店界面方案保留 v0.1。

---

## 二、规则层改动（`Project/scripts/game_state.gd` + `card_database.gd`）

### 1. 抽弃牌规则

- `TURN_DRAW: 2 → 6`（保留 `FIRST_TURN_DRAW = 6` 兼容旧测试）
- `_start_turn` 简化为统一抽 6 张
- `_settle_turn` 开头新增：未打出手牌按 `hand[0..n-1]` 顺序逐张 append 到 `discard_pile`，再清空 hand
  - `hand` 数组顺序天然即抽牌顺序（早抽 index 小），满足"先抽到的先进弃牌堆"
- 已打出的牌仍在 `play_card` 中立即进弃牌堆

### 2. 首回合"抽到 7 张"bug 修复

- 起因：原实现"先抽 6 → 缺类型再 `hand.append()` 补一张"，缺技能时会变 7 张
- 改法：整局第一回合改为**先种 1 买 + 1 卖 + 1 技能，再补到 6 张**
- 保证：第一回合手牌恒为 6，且三类齐全

### 3. 初始卡组配比

- 旧：3 买 + 3 卖 + 3 内幕 + 3 大V吹票（4×3）
- 新：**5 买 + 5 卖 + 1 大V吹票 + 1 内幕消息**，总数仍 12 张

### 4. 买卖牌效果重写

| 卡 | 旧效果 | 新效果 |
|----|--------|--------|
| 买入 | 消耗现金 10% 买入，不影响股价 | **固定买 100 股，股价 +1%** |
| 卖出 | 卖出持仓 10%，不影响股价 | **固定卖 100 股，股价 -1%** |

- 新增 `_buy_shares(n)`（**顺便修了原代码调用一个不存在函数的 nil bug**）
- `play_card` 加资源前置检查：
  - 买入要求 `cash ≥ 100 × price`
  - 卖出要求 `shares ≥ 100`
  - 不满足直接拒绝出牌，**不扣 AP、不进弃牌堆**
- 卡牌描述文案同步更新

### 5. 突发事件系统（新增）

- 信号：新增 `event_triggered(event: Dictionary)`
- 字段：新增 `current_event: Dictionary`、`banned_effect_ids: Dictionary`
- 触发时机：`EVENT_TRIGGER_TURNS = [1, 5]`，每天第 1 / 第 5 回合开盘抽牌后刷新
- 事件池 `EVENT_POOL`（8 条等概率抽 1，必出）：
  - 利好1/2/3 → 情绪 +10/+20/+30（一次性）
  - 利空1/2/3 → 情绪 -10/-20/-30（一次性）
  - 网络监管 → ban `hype_basic` + `hype_plus`
  - 收口如瓶 → ban `insider_basic` + `insider_plus`
- Ban 持续到下次事件刷新；新一天 `_start_day` 自动清掉残留 ban
- `play_card` 在 AP 检查后判 ban，被禁卡牌不扣 AP 拒绝出牌

### 6. 每天重置市场情绪

- `_start_day` 开盘时把 `bull / bear` 拉回初始中性值（`INITIAL_BULL = 50` / `bear = 50`）
- 上一天累计的利好/利空情绪不会带到新一天

### 7. 默认股价震荡（恢复，按策划 4.3）

- `_settle_turn` 回合末重新滚一次自然波动
- `drift ~ Normal(μ, σ)`，`μ = (bull - 50) / 50 × 10%`，`σ = 1.2%`（暂定，策划 4.3 标注待数值组确认）
- `NATURAL_DRIFT_CLAMP` 由 3% 改为 **10%**，对齐策划
- 调用 `apply_price_change(drift, true)`：传 `ignore_emotion_modifier=true`，因为 μ 本身已经体现情绪方向，不再叠加情绪倍率
- 生成一根 `kind:"settle"` 分时 K，写日志 `回合末自然波动 +x.xx% → ¥xxx.xx`

### 8. BGM 接入

- `_ready` 末尾启动 `AudioStreamPlayer` 播放 `res://assets/bgm/Measured Inflection.mp3`
- 把 `AudioStreamMP3.loop` 置 true 实现循环
- 初始音量 -6dB，节点挂在主场景上随场景生命周期销毁
- 需 Godot 编辑器首次打开生成 `.import`，否则 `load()` 失败

---

## 三、UI 层改动（`Project/scripts/main.gd`）

### 1. 左侧资金条整体重做

旧：四色段（蓝/绿/黄/红）+ 白色现金 marker + 右侧数据面板内的竖向黄色目标进度条

新：**总资金条**
- 黑底，满刻度 `START_CASH × 2 = 200K`
- **蓝段** 从底向上 = 现金
- **黄段** 叠在蓝段上方 = 持仓市值（"浮盈"含本）
- 顶部白色横线 = 胜利目标位（`VICTORY_TARGET / 200K = 60%` 高度），左侧"目标 ¥120,000"小字
- 右侧数据面板的竖向目标进度条 + `lbl_target` 全部移除，仅保留"总资产"数字

### 2. 突发事件接入

- 顶栏"突发事件"按钮加宽到 220px，常驻显示当前事件
  - 文案：`今日事件 · 利好2` / `今日事件 · 网络监管` / 无事件 `今日事件 —`
  - 字色：利好绿 / 利空红 / Ban 金 / 空灰
- `_on_event_triggered`：在日志区追加一行金色 `[突发事件] 名称 — 描述`
- `_refresh_state` 每次刷新都被动同步 `Game.current_event`，新一天会自动归位
- 手牌按钮 disable 条件追加 `Game.banned_effect_ids.has(c.effect_id)`，被 ban 的卡自动灰显（两处出牌按钮均覆盖）

---

## 四、文件影响清单

| 文件 | 变化 |
|------|------|
| `docs/新乡2000_UI界面设计方案.md` | 整体重写为 v0.2 |
| `docs/新乡2000_UI组件拆分建议.md` | 整体重写为 v0.2 |
| `Project/scripts/game_state.gd` | 抽弃牌、首回合保底、买卖效果、突发事件、每日重置情绪、自然波动恢复（clamp ±10%） |
| `Project/scripts/card_database.gd` | 初始卡组配比 + 买卖牌描述 |
| `Project/scripts/main.gd` | 总资金条重做、突发事件 UI 接入、手牌 ban 灰显、BGM 接入 |
| `Project/assets/bgm/Measured Inflection.mp3` | 新增 BGM 资源 |

### 兼容性

- `Project/tests/rule_smoke_test.gd` 仍满足："牌组=12"和"第一回合至少 6 张"两条断言均通过
- `FIRST_TURN_DRAW` 常量保留，未破坏旧测试引用

---

## 五、Phase 2 / Phase 3 累积改动

### 1. A 股配色翻盘 + 背景情绪渐变

- `COL_UP` 改红 `#ef476f`，`COL_DOWN` 改绿 `#06d6a0`
- 影响范围：K 线、`_kind_color`（BUY→红 / SELL→绿）、顶栏情绪条、P&L、价格变化字色（全部自动跟随）
- `bg` 全屏 `ColorRect` 提升为字段 `bg_rect`，在 `_refresh_state` 末尾按情绪插值：
  - 中性 bull=50 → `COL_BG`
  - bull→100 插值到 `COL_BG_BULL_MAX = #a01e3f`（鲜红高饱和）
  - bull→0  插值到 `COL_BG_BEAR_MAX = #0e8a52`（鲜绿高饱和）

### 2. 价格图表 + 右侧信息栏半透明

- 新增 `_panel_stylebox_translucent(alpha=0.55, border)`，复用原 stylebox 形状仅降低 `bg_color.a`
- `_build_chart_area` / `_build_right_panel` 创建后覆盖为半透明
- 其他面板（顶栏、左侧资金条、底部行动力、手牌、商店）保持不透明，避免高密度文字区可读性下降
- `_on_draw_chart` 内部仅画边框/线/字，不填底色，保证背景能透出

### 3. 突发事件系统重写（25 条）

事件数据/逻辑从 `game_state.gd` 拆出，模仿 `card.gd` / `card_database.gd` 风格新增：

- `Project/scripts/event.gd` — `Event` 数据类（RefCounted），含 `Category` 枚举 (GOOD/BAD/NEUTRAL) + 字段 + `is_*()` / `category_str()` 辅助
- `Project/scripts/event_database.gd` — `EventDatabase` 静态工厂，提供 `build_event_pool()` / `make_by_id()` / `all_event_ids()`

事件池扩到 25 条（原 8 条被替换）：

| 类型 | 事件 |
|------|------|
| 利好情绪 | 降息 +10 / 外盘暴涨 +20 / 黄金时代 +30 |
| 利空情绪 | 加息 -10 / 外盘暴跌 -20 / 战争风险 -30 |
| 股价直击 | 盈利双击 +20% / 黑天鹅 -70% |
| 情绪锚定 | 信仰充值（≥50）/ 信仰崩塌（≤50） |
| 持续 3 回合 | 超预期财报（+5 且涨幅 ×1.2）/ 财报逆袭（-5 且跌幅 ×1.2） |
| 长期修饰 | 火线预期 / 下行预期 / 杠杆涨市（成交价格 ×1.5）/ 风险警示（情绪倍率 ×0.5）/ 市场失真（脱钩） |
| 涨跌停 | 限制涨停 ≤5% / 限制跌停 ≤5% |
| 牌库 / AP | 账户审查（每回合冻结 1 张）/ 重点监控（每回合最多 2 张技能）/ 混乱之日（每回合 50% AP+1 / 50% AP-1） |
| 其他 | 神秘资金（回合末 30% 概率 ±5%）/ 意外事件（情绪随机 ±10） |

### 4. 事件 modifier 通用机制

`game_state.gd` 新增状态：`event_modifiers: Dictionary` / `event_modifier_dur: int` / `triggered_event_ids_this_level: Dictionary` / `skills_played_this_turn: int`。

各 modifier 落地点：

| modifier 键 | 落地位置 | 行为 |
|-------------|----------|------|
| `card_price_up_mul` / `card_price_down_mul` | `_dispatch_effect` 技能牌方向倍率 | 价格变化方向放大 |
| `card_trade_price_mul` | `_dispatch_effect` 买/卖牌 | 成交引发的价格变化 ×1.5 |
| `emotion_modifier_mul` | `_emotion_modifier_for_price` 末尾 | 情绪倍率整体 ×0.5 |
| `decouple` | `_emotion_modifier_for_price` 返回 1.0 + `_roll_natural_drift` 中 μ 置 0 | 卡牌与自然波动均与情绪脱钩 |
| `emotion_floor` / `emotion_ceiling` | `apply_emotion_delta_bull` clamp | 当时段情绪上下限 |
| `cap_drift_up` / `cap_drift_down` | `apply_price_change` 末尾 | 累计涨/跌幅相对回合开盘价封顶 |
| `freeze_per_turn` | `_start_turn` | 每回合随机弃 N 张手牌进弃牌堆 |
| `skill_cap_per_turn` | `play_card` 前置检查 | 本回合技能牌使用上限 |
| `mystery_active` | `_settle_turn` 自然波动叠加 | 30% 概率额外 ±5% |
| `ap_chaos` | `_start_turn` 末尾 | 每回合 50% AP+1 / 50% AP-1（含触发回合） |

### 5. 事件刷新规则

- 每天第 1 / 第 5 回合开盘抽牌后调用 `_trigger_random_event()`
- 一周内同事件不重复：抽样前按 `triggered_event_ids_this_level` 过滤；整池跑完后兜底退回完整池
- 新事件刷新时清空上一事件的 `banned_effect_ids` / `event_modifiers` / `event_modifier_dur`
- 短期事件用 `dur_turns`（如超预期财报 / 财报逆袭 = 3）；`_settle_turn` 末尾倒计时，归零时清空并发 `event_triggered.emit(null)` 让 UI 复位
- `_start_day` 清当天事件残留；`new_level` 同时清 `triggered_event_ids_this_level`（"一周不重复"等于一关不重复）

### 6. 事件 UI

- 事件触发弹窗：`AcceptDialog`，标题"突发事件 · 利好/利空/中性"按 `category` 染色，正文 = 事件名 + 描述 + 效果文案；点"知道了"关闭
- 顶栏"今日事件"按钮颜色：利好红 / 利空绿 / 中性金 / 空灰；dur_turns 到期发空载荷时自动复位
- 鼠标悬停按钮弹出下拉信息栏：
  - `event_tip_panel: PanelContainer`（顶层 `top_level=true`、`z_index=100`、`mouse_filter=IGNORE`）
  - 三行：标题（带"[利好/利空/中性]"标签）+ 描述（autowrap）+ 效果（autowrap）
  - 无事件时显示"暂无突发事件，每天第 1 / 第 5 回合刷新"

### 7. 信号 / 类型变化

- `signal event_triggered(event)` 载荷统一为 `Event` 实例或 `null`
- `current_event: Event = null`（原 `Dictionary = {}`）
- 删除 `game_state.gd` 中的 `const EVENT_POOL`（整段迁出）

### 8. 文件影响清单（增量）

| 文件 | 变化 |
|------|------|
| `Project/scripts/event.gd` | 新增：Event 数据类 |
| `Project/scripts/event_database.gd` | 新增：25 条事件池 + 静态工厂 |
| `Project/scripts/game_state.gd` | 删除 `EVENT_POOL`；新增 modifier 通用机制；重写 `_trigger_random_event` / `_apply_event_effects`；`apply_price_change` 加涨跌停封顶；`_emotion_modifier_for_price` 加 decouple / 风险警示；`_roll_natural_drift` 加 decouple 时 μ=0；`_start_turn` 加账户审查 / 混乱之日 / 技能计数重置；`play_card` 加重点监控上限；`_settle_turn` 加神秘资金 / dur_turns 倒计时 |
| `Project/scripts/main.gd` | A 股翻色 + 情绪背景渐变；价格图表 / 右侧面板半透明；事件弹窗 + 顶栏按钮颜色 + 悬停下拉 tip；`_on_event_triggered` / `_refresh_event_button` 改 Event 字段访问 |